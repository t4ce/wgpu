[package]
name = "wgpu-trueos-hal-probe"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
wgpu-hal = { path = "../../../wgpu-hal", default-features = false, features = ["trueos"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("trueos"))'] }
